{
  "name": "softbakery",
  "version": "2.0.0",
  "description": "Software para o gerenciamento de uma padaria",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --env-file=.env server.js",
    "start": "node server.js",
    "build": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "brunofsz",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.18.0",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
